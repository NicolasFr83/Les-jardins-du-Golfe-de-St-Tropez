<form method="post" action="{{ path('app_opening_delete', {'id': opening.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ opening.id) }}">
    <button class="btn">Delete</button>
</form>
