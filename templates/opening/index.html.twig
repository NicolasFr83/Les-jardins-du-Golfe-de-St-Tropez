
<div class="openingSection__days">
    <img src="{{ asset('images/Logo-enterprise.png')}}" alt="logo-entreprise" class="openingSection__logo" >
    <div class="openingSection__open">
        <a class="openingSection__title">Téléphone : 04 98 33 00 00</a>
        <p class="openingSection__title">Ouverture : </p>
        <ul>
            {% for opening in opening %}
                <li>{{ opening.openingDay }} : {{ openingHourMorning }} - {{ closingHourMorning }} {{ openingHourAfternoon }} - {{ closingHourAfternoon }}</li>
            {% endfor %}
        </ul>
    </div>
</div>