{% extends 'base.html.twig' %}

{% block title %}HomePage{% endblock %}

{% block body %}
    <h1>HomePage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home_page.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ home_page.title }}</td>
            </tr>
            <tr>
                <th>PresentationText</th>
                <td>{{ home_page.presentationText }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_home_page_index') }}">back to list</a>

    <a href="{{ path('app_home_page_edit', {'id': home_page.id}) }}">edit</a>

    {{ include('home_page/_delete_form.html.twig') }}
{% endblock %}
