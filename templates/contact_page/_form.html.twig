<section class="contact-page-form__flux"> 
    {{ form_start(form, {'attr': {'class': 'contact-page-form__form'}}) }}
        {{ form_widget(form) }}
        <div class='contact-page-form__button-div'>
            <button class='contact-page-form__button-div--button'>{{ button_label|default('Sauvegarder') }}</button>
        </div>
    {{ form_end(form) }}
</section> 