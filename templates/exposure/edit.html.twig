{% extends 'base.html.twig' %}

{% block title %}Edit Exposure{% endblock %}

{% block body %}
    <h1>Edit Exposure</h1>

    {{ include('exposure/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_exposure_index') }}">back to list</a>

    {{ include('exposure/_delete_form.html.twig') }}
{% endblock %}
