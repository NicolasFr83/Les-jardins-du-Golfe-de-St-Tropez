{% extends 'base.html.twig' %}

{% block title %}Enterprise index{% endblock %}

{% block body %}
    <h1>Enterprise index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>PhoneNumber</th>
                <th>Email</th>
                <th>Adress</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for enterprise in enterprises %}
            <tr>
                <td>{{ enterprise.id }}</td>
                <td>{{ enterprise.name }}</td>
                <td>{{ enterprise.phoneNumber }}</td>
                <td>{{ enterprise.email }}</td>
                <td>{{ enterprise.adress }}</td>
                <td>
                    <a href="{{ path('app_enterprise_show', {'id': enterprise.id}) }}">show</a>
                    <a href="{{ path('app_enterprise_edit', {'id': enterprise.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_enterprise_new') }}">Create new</a>
{% endblock %}
