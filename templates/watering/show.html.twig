{% extends 'base.html.twig' %}

{% block title %}Watering{% endblock %}

{% block body %}
    <h1>Watering</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ watering.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ watering.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_watering_index') }}">back to list</a>

    <a href="{{ path('app_watering_edit', {'id': watering.id}) }}">edit</a>

    {{ include('watering/_delete_form.html.twig') }}
{% endblock %}
